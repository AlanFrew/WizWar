using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace WizWar1 {
class AroundTheCorner : Spell { //IListener<AdditionalInfoUltimatum, Event> {
    //public AroundTheCorner() {
    //    Name = "Around The Corner";
    //}

    //public void OnEvent(AdditionalInfoUltimatum tEvent) {
    //    if (tEvent.controlFlowEvent == ControlFlowState.FindTarget) {
    //        tEvent.SetFlowControl = RedirectSet.Deny;
    //    }

        //Square tSquare = Caster.myUI.myForm.FindSelectedSquare(); //beware thread sync issues
        //if (GameState.BoardReference.TestLoSNew(tSquare.X - GameState.active_player.X, tSquare.Y - GameState.active_player.Y) == false) {
        //    try {
        //        if (GameState.BoardReference.TestLoSNew(Double.Parse(maskedTextBox1.Text), Double.Parse(maskedTextBox2.Text)) == false) {
        //            MessageBox.Show("No line of sight");
        //            return;
        //        }
        //    }
        //    catch (FormatException) {
        //        MessageBox.Show("No line of sight");
        //        return;
        //    }
        //}
}
}
