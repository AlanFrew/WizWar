using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace WizWar1 {
class PartialCounterEffect : Effect {
    public override void OnRunChild() {
        //GameState.instantEffectStack.Remove(target as Effect);
        //GameState.durationEffects.Remove(target as Effect);
    }
}
}
